<%= render 'shared/menu' %>

<div class='page_panel'>
   <%= render 'products/productPagesMenu' %>
   <div class='main_content'>
      <h1>
      <%= @product.title %>
      </h1>

      <p>
      <strong>Status:</strong>
      <%= @product.status %>
      </p>

      <p>
      <strong>Features Supported:</strong>
      <%= @product.supportedFeatures %>
      </p>
   </div>
</div>
